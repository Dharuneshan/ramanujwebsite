<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<title>Admin Login</title>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
	<style>
		body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, sans-serif; background:#0f172a; color:#e2e8f0; display:flex; align-items:center; justify-content:center; min-height:100vh; margin:0; }
		.card { background:#111827; padding:32px; border-radius:16px; box-shadow: 0 10px 30px rgba(0,0,0,0.4); width: 100%; max-width: 380px; }
		.h1 { font-weight:700; font-size: 24px; margin:0 0 16px; color:#93c5fd }
		label { display:block; font-weight:600; margin: 16px 0 8px; color:#94a3b8 }
		input { width:100%; padding:12px 14px; border-radius:10px; border:1px solid #334155; background:#0b1220; color:#e2e8f0 }
		button { width:100%; margin-top:20px; padding:12px 16px; border-radius:10px; border:0; background:#4f46e5; color:#fff; font-weight:700; cursor:pointer }
		.error { color:#f87171; margin-top:12px; }
		.footer { text-align:center; margin-top:12px; color:#64748b; font-size:12px }
	</style>
</head>
<body>
	<div class="card">
		<div class="h1">Admin Login</div>
		<form method="post" action="/auth/login">
			<label>Email</label>
			<input type="email" name="email" required />
			<label>Password</label>
			<input type="password" name="password" required />
			<button type="submit">Sign in</button>
			<% if (error) { %>
				<div class="error"><%= error %></div>
			<% } %>
		</form>
		<div class="footer">Use the seeded superuser credentials.</div>
	</div>
</body>
</html>


